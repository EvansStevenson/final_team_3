<%- include('./includes/head.ejs') %>
</head>

<body>
    <%- include('./includes/nav.ejs') %>
</header>
        <h1 class="">Welcome <%= user.name %></h1>
        <div class="">
            <% if (foods.length > 0) { %>
                <div class="">
                    <h2></h2>
                    <% for (let recipe of recipes) { %>
                       
                        <h2 class="">Recipes</h2>
                            <h4 class=""><a href="/recipes/<%=recipe._id%>"><%= recipe.title %></a><a class="btn btn-primary" href="/recipes/<%=recipe._id%>/edit">Edit</a><a class="btn btn-danger" href="/recipes/<%=recipe._id%>/delete">Delete</a></h4>
                    <% } %> 
                            
                            <!-- LEFT ARROW THUMBS UP -->
                            <div>
        
                            </div>
                            <!-- RIGHT ARROW THUMBS DOWN-->
                        <h2 class=""><a href="/auth/<%= user._id%>/edit">Edit Account</a></h2>
                     <div class="buttons">
                         <button type="submit" class="button">Add to Favorites</button>
                        <!-- <button type="submit" class="button">Add to List</button> -->
                     </div>
                </div>  
        </div>
            <% } else { %>
                <h1>No Foods Found</h1>
            <% } %>
<%- include('./includes/end.ejs') %>